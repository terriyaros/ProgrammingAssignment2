create a file and subdirecty and file path
| dir.create(file.path('testdir2', 'testdir3'), recursive =
| TRUE) will do the trick. If you forgot the recursive
| argum

git add --all (add changed files to be committed)

git commit -m "some message" (commits the files to git with that message)

git push (pushes everything into github)